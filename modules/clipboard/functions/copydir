#
# Copies the pathname of the current directory to the system or X Windows clipboard
#
# Taken from https://github.com/robbyrussell/oh-my-zsh/blob/master/plugins/copydir
function copydir {
  emulate -L zsh
  print -n $PWD | clipcopy
}
